# Core web framework
fastapi==0.115.2
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# Data validation and settings
pydantic==2.9.2
pydantic-settings==2.6.0

# Task queue
celery==5.4.0
redis==5.0.8

# Templates and auth
Jinja2==3.1.4
python-jose[cryptography]==3.3.0

# HTTP and monitoring
httpx==0.27.2
prometheus-fastapi-instrumentator==6.1.0
email-validator==2.2.0

# Utilities
pyyaml==6.0.2
psutil==6.0.0
python-dotenv==1.0.1

# Audio processing
pyannote.audio==3.3.2
soundfile==0.12.1
librosa==0.10.2

# Machine learning (CPU optimized)
numpy==1.26.4
faster-whisper==1.0.3
transformers>=4.40.0,<5.0.0
accelerate>=0.20.0
datasets>=2.0.0

# PyTorch (CPU only - installed separately in Dockerfile to avoid memory issues)
# torch==2.5.1 (CPU version via --index-url)
# torchaudio==2.8.0 (latest version from PyPI)

# Document generation
reportlab>=4.0.0
python-docx>=0.8.11
python-pptx>=0.6.21
